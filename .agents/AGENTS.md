# Serenity Project Behavior Rules

## Version Control Rules
- 每次更新一個大功能或者修改 bug，都必須執行 `git add`、`git commit` 以及 `git push` 把變更同步到 GitHub 遠端倉庫。
- 每次 commit 應使用清晰、符合規範的提交訊息（例如：`feat: ...` 或 `fix: ...`）。
